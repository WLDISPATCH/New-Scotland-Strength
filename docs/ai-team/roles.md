PM Agent → writes/refines issues + acceptance criteria

Architect Agent → technical approach + constraints

Builder Agent → implements code + tests

QA Agent → validates criteria + edge cases

Ops Agent → merge/release/changel

one agent = one responsibility

builder cannot self-approve

low-confidence tasks escalate to human
